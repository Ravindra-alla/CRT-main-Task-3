services:
  - type: web
    name: healthcare-prediction-system
    env: python
    buildCommand: pip install -r requirements.txt && python seed_test_data.py
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
